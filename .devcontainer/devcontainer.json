{
  // Use Docker Compose
  // "dockerComposeFile": "docker-compose.yml",
  // "service": "dev",
  // "overrideCommand": true,
  // End Use Docker Compose
  // Use only devcontainer.json
  "image": "perfsim-docker",
  "name": "VPU Perfsim Docker",
  "workspaceMount": "source=${localWorkspaceFolder},target=/home/${localEnv:USER}/${localWorkspaceFolderBasename},type=bind",
  "mounts": [
    "source=/tmp/.X11-unix,target=/tmp/.X11-unix,type=bind,consistency=cached",
    "source=/dev/dri,target=/dev/dri,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.ssh,target=/home/${localEnv:USER}/.ssh,type=bind,consistency=cached"
  ],
  "runArgs": [
    "--net=host",
    "--pid=host",
    "--ipc=host",
    "--hostname=vpu-perfsim",
    "--add-host=vpu-perfsim:127.0.0.1",
    "--name=vpu-perfsim",
    "-e",
    "DISPLAY=${env:DISPLAY}"
  ],
  // End Use only devcontainer.json
  // Shared settings
  "containerEnv": {
    "DISPLAY": "unix:0",
    "WS_PATH": "/home/${localEnv:USER}/${localWorkspaceFolderBasename}"
  },
  "workspaceFolder": "/home/${localEnv:USER}/${localWorkspaceFolderBasename}",
  "remoteUser": "${localEnv:USER}"
  // "customizations": {
  //   "vscode": {
  //     "extensions": [
  //       "ms-vscode.cpptools",
  //       "ms-vscode.cmake-tools",
  //       "xaver.clang-format",
  //       "ms-python.python",
  //       "ms-python.black-formatter",
  //       "rioj7.command-variable",
  //       "mhutchie.git-graph",
  //       "Gruntfuggly.todo-tree"
  //     ]
  //   }
  // }
}
